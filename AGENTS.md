# AGENTS.md instructions

## 语言

- 所有回复使用中文。

## 环境与命令

- 默认运行环境为 Windows。
- 优先使用 PowerShell 命令。
- 不使用 `rg`；文本和文件检索使用 PowerShell 的 `Get-ChildItem`、`Select-String` 等命令。

## 文件修改

- 修改前先阅读与当前任务直接相关的现有文件和项目规则。
- 使用项目现有的技术栈、代码风格和目录结构。
- 完成修改后，说明本次变更涉及的文件及验证结果。
- 不修改与当前任务无关的文件。

## PRD 文档

- 处理需求前，必须先读取 `docs/README.md`，通过索引按需加载与当前任务相关的 PRD。
- 需求发生变更时，必须同步更新对应的 PRD 文档，并更新其最后更新日期、状态或索引摘要等受影响信息。
- 新增、移动或废弃 PRD 时，必须同步更新 `docs/README.md` 中的目录。

## Codex 行为偏好

- 修改文件后，简要说明改了哪些文件、完成了什么。
- 每次生成或更新 HTML 原型/HTML 文件后，不默认在右侧内置浏览器打开预览；需在回复中提供文件路径或预览入口，用户明确需要打开时再打开。
- 涉及代码、脚本、配置时，尽量保持改动小而明确。
- 需要联网、访问外部资料或使用额外权限时，先说明原因。
- 若发现已有项目规范、README 或更深层目录中的 `AGENTS.md`，优先遵守更具体的规则。

## PRD 文档规则

- 当生成或更新 PRD 时，必须包含以下部分：
  1. 背景与目标
  2. 用户角色
  3. 使用场景
  4. 需求范围
  5. 功能流程
  6. 页面说明
  7. 字段说明
  8. 交互规则
  9. 异常场景
  10. 埋点需求
  11. 验收标准
  12. 不做范围
- 所有需求描述必须尽量避免模糊表达，例如“优化体验”“提升效率”；需要说明具体指标、触发条件、业务场景或验收口径。
- PRD 只描述最终展示效果和最终业务规则，不记录历史调整过程；页面无需呈现的内容，不在 PRD 中作为展示项列出。

## UX 规则

- 设计功能时必须考虑以下状态：
  - 默认状态
  - 加载状态
  - 空数据状态
  - 错误状态
  - 权限不足状态
  - 网络异常状态
  - 操作成功状态
  - 操作失败状态
- 所有按钮、弹窗、Toast、错误提示都需要给出建议文案。

## Edge Cases 规则

- 需求分析必须考虑以下边界与异常场景：
  - 用户无权限
  - 数据为空
  - 数据过多
  - 网络失败
  - 重复提交
  - 并发操作
  - 超时
  - 撤销或取消
  - 旧版本兼容
  - 多端同步
  - 异常数据

## 产品文案规则

- 产品文案要求：
  - 简洁、明确、可执行
  - 避免使用技术术语
  - 避免使用模糊表达
  - 错误提示需要告诉用户原因和解决方式
  - 按钮文案使用动词开头
- 示例：
  - 不推荐：操作失败
  - 推荐：订单导出失败，请稍后重试

## 不确定性处理规则

- 当需求中存在不明确的信息时：
  - 不要自行假设为最终结论
  - 可以基于合理假设继续分析
  - 必须显式标注“假设”
  - 必须列出“待确认问题”
- 格式示例：

  假设：
  - 假设用户已经完成实名认证

  待确认：
  - 是否支持未实名认证用户下单？
  - 是否需要短信通知？

## 研发协作规则

- 面向研发输出需求时，需要包含：
  - 功能目标
  - 页面入口
  - 业务规则
  - 接口依赖
  - 字段说明
  - 权限规则
  - 状态流转
  - 异常处理
  - 验收标准
- 不要只描述页面效果，需要说明业务逻辑。

## 变更与不确定性规则

- 未下达明确需求，或需求中存在不明确的信息时，不能自行假设为最终结论，也不能自行进行未被要求的优化。
- 可以基于合理假设继续分析，但必须按“不确定性处理规则”显式标注“假设”和“待确认问题”。
- 每次涉及 PRD、原型、项目规则或索引的变更，都必须在 `E:\Workspace\docs\README.md` 的变更记录中写明变更日期、变更对象和变更说明。

## Git

- 未经用户明确要求，不执行 `git commit`、`git push`、`git reset`、`git clean` 等 Git 操作。
- 执行可能丢失数据或覆盖历史的 Git 操作前，必须获得用户明确确认。
- 操作前注意保留工作区中已有、与当前任务无关的变更。
- 开始修改前应检查工作区状态，明确本次变更范围，不混入无关文件。
- 每项可交付的需求、修复或文档更新应使用独立提交保存，提交前确认变更内容与需求一致。
- 提交信息使用简洁、可追溯的格式：`类型: 变更摘要`，例如 `feat: 新增订单导出`、`docs: 更新会员 PRD`；类型可使用 `feat`、`fix`、`docs`、`refactor`、`chore`。
- 新功能、修复和文档维护应在独立分支完成；分支名称使用 `feat/`、`fix/`、`docs/` 或 `chore/` 前缀，并包含简短主题。
- PRD、原型、项目规则和索引的变更应与对应实现关联；提交或合并说明中需注明关联需求或 PRD 路径。
- 重要发布版本使用语义化标签 `v主版本.次版本.修订版本`；不兼容变更递增主版本，新增兼容功能递增次版本，兼容修复递增修订版本。
- 不提交密钥、令牌、个人配置、构建产物或其他不应共享的文件；通过 `.gitignore` 管理此类文件。
